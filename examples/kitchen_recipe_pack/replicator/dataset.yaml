version: "1.0"
metadata:
  name: kitchen_manipulation_dataset
  created_at: "2024-01-15T10:30:00Z"
  generator: BlueprintRecipe
  policy: Dexterous Pick & Place

scene:
  path: ../scene.usda
  up_axis: Y
  meters_per_unit: 1.0

generation:
  num_frames: 1000
  seed: 42
  headless: true
  use_gpu: true

output:
  directory: ./output
  format: replicator_default
